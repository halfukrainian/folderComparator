I tend to backup my files a lot and sometimes it’s hard to tell what’s different. So, I wrote this little script to compare two directories and list out the files that don’t match. It uses common file comparison methods, so it’s nothing groundbreaking, but it gets the job done.